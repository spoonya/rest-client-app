export enum AppRoutes {
  HOME = '/',
  HISTORY = '/history',
  REST = '/rest-client',
  VARS = '/variables',
  SIGN_IN = '/sign-in',
  SIGN_UP = '/sign-up',
  AUTH = 'auth',
}

export const locales = ['en', 'ru'] as const;
